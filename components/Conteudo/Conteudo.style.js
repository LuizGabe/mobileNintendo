import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: "center",

    padding: 30
  },
  
  title: {
    textAlign: "center",
    fontSize: 18,
    fontWeight: "bold",

    marginBottom: 10
  },

  desc: {
    textAlign: "center",

    marginBottom: 30
  },

  image: {
    width: 200,
    height: 120,

    marginBottom: 10
  }
});

export default styles;